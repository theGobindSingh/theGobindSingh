import { css } from '@emotion/react';

const globalStyles = css`
  :root {
    --some-variable: #000;
  }
  body {
    color: black;
    padding: 0;
    margin: 0;
    box-sizing: border-box;
  }
`;

export default globalStyles;
